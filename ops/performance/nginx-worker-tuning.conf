# Include from the main nginx.conf after measuring the host.
# Keep one API process on the 2 GiB baseline; Nginx handles static assets and
# TLS while the API keeps a bounded database pool.
worker_processes 2;
worker_rlimit_nofile 4096;

events {
    worker_connections 1024;
    multi_accept off;
}
